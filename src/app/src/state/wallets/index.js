import reducer from './reducers';

import * as walletsOperations from './operations';
import * as walletsSelectors from './selectors';
import * as walletsTypes from './types';

export { walletsOperations, walletsSelectors, walletsTypes };

export default reducer;
